package com.example.childtrackerapp.utils

import androidx.compose.ui.graphics.Color

val PrimaryLight = Color(0xFFD1C4E9)      // tím nhạt
val Primary = Color(0xFF7E57C2)           // tím chính
val PrimaryDark = Color(0xFF5E35B1)       // tím đậm hơn
val PrimaryDarker = Color(0xFF311B92)     // tím đậm nhất

// Màu chữ
val TextPrimary = Color(0xFF212121)       // văn bản chính (giữ đen để dễ đọc)
val TextSecondary = Color(0xFF9575CD)     // văn bản phụ, dùng tím nhạt
// Màu nền
val BackgroundDefault = Color(0xFFFFFFFF)     // nền mặc định
val BackgroundSecondary = Color(0xFFF5F5F5)     // nền light
val BackgroundSelected = Color(0xFFE3F2FD)    // nền khi chọn, ví dụ tab được chọn
